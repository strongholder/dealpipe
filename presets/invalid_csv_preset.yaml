solids:
  load_deals:
    config:
      deals_file: "files/invalid.csv"
  load_deals_lookup:
    config:
      lookups_file: "files/lookups.csv"
  save_errors:
    config:
      errors_excel_file: "output/deals_errors.xlsx"
  save_output:
    config:
      output_file: "output/deals.parquet.gz"
      compression: "gzip"
